---
"@jawclaw/channels": minor
"jawclaw": minor
---

feat: add channel extension system — external npm packages can now provide Channel implementations via a standardized ChannelExtension contract, registry, and dynamic loader
